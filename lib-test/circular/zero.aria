# SPDX-License-Identifier: Apache-2.0
import circular.one;

struct Zero {
    type func name() {
        return "Zero";
    }
    
    type func wrapped_name() {
        return circular.one.One.name();
    }
}